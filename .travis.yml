language: node.js
node_js:
  - "10"
cache:
  directories:
    - node_modules
    - client/node_modules
install:
  - npm install
  - npm run build --prefix client
script:
  - npm run build --prefix client
  - npm run test --prefix client